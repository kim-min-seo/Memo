spring:
  mvc:
    static-path-pattern: /static/**
  config:
    import: security.yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/memo_minse0
    username: root
    password: ${mysql.password}
  thymeleaf:
    cache: false
  application:
    name: Memo
mybatis:
  mapper-locations: mappers/*Mapper.xml